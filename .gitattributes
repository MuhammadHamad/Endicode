# Normalize text files to LF in the repo (project history uses LF).
* text=auto eol=lf

# Treat binary assets as binary so they're never line-ending munged.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.woff binary
*.woff2 binary
*.ttf binary
*.sqlite binary
*.db binary
